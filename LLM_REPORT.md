# LLM_REPORT

## 1. 도구와 사용 방식

**Cursor**로 스펙을 읽힌 뒤, 기능을 커밋 단위로 나눠 구현을 위임했다.

흐름은 이렇다.

1. 과제 문서(R1–R4, 부록 A)를 기준으로 다음 할 일만 짧게 정한다.
2. UX·상태 모양(`draft`/`sessions`, 면적 닫기, store 분리)은 대화에서 먼저 고정한다.
3. 코드 작성·브라우저 확인·수정은 LLM에 맡긴다.
4. 결과가 명세나 GPU 제약과 안 맞으면 기각하고 다시 지시한다.

프로토타입을 통째로 받은 뒤 리팩터한 방식은 아니다. **한 기능씩 범위를 잠그고 구현**했다.

## 2. 역할 구분

| 파일/모듈 | 작성 주체 | 비고 |
| --- | --- | --- |
| `pick.ts`, `HoverPick.tsx` | 방식은 직접 선택, 코드는 LLM | Raycaster/splat 중심점 대신 GPU 깊이+알파 누적 |
| `Measure.tsx` | LLM 초안 → 반복 수정 | 입력·고스트·Line2·면적 닫기는 지시로 고정 |
| `useMeasureStore.ts` | 설계는 직접, 코드는 LLM | `draft`/`sessions`, `WORLD_TO_METER` |
| `useToolStore.ts` | 직접 분리 지시 | 측정 상태와 UI 토글 주기 분리 |
| `SplatModel.tsx` | LLM | 로드·회전·프레이밍. 스케일 변환 없음 |
| `OrbitPivot.tsx` | LLM, UX는 직접 | Ctrl+클릭 피벗. `AxesHelper` 기각 후 Line2 |
| Overlay (`Toolbar`, `Guide`, `SessionList`, `FileBar`) | LLM, 배치·조작은 직접 | 목록/이름/삭제/바로가기 |
| `DESIGN.md` / `README.md` / 본 파일 | LLM 초안, 실측·스케일 판단은 본인 | 측정값은 뷰어에서 직접 기록 |

## 3. LLM이 틀렸던 지점

과제 5-2는 “무엇이 잘못됐고 / 어떻게 알아챘고 / 어떻게 고쳤는지”를 본다. 채팅에서 그 세 칸이 가장 뚜렷한 순이다.

### three.js 기본 `raycast`로 호버

첫 호버는 `pointermove`마다 `GaussianSplat.raycast`를 돌렸다. r186 구현은 splat 전수 루프라, 약 300만 개 PLY에서 **CPU 100%**가 됐다. 작업 관리기로 바로 보였다.

그다음 LLM 안은 CPU 해시그리드 + 타원체 교차였다. 줄 수는 늘고, 화면에 넓게 퍼진 splat 가장자리는 miss했다. 보이는 표면을 재는 과제 UX와 안 맞아 경로를 폐기했다.

`splat.raycast = () => {}`로 막고, 그린 알파를 누적한 viewZ를 언프로젝션하는 쪽으로 바꿨다. 이후 호버/측정은 같은 픽을 쓴다.

### `setViewOffset` 1px — 가우시안 **중심만** 스냅

`camera.setViewOffset(..., 1, 1)`로 커서 한 픽셀만 렌더하는 안을 받았다. 화면에서는 splat이 넓게 보이는데, 호버는 **타원 중심**에만 붙었다. `GaussianSplat`이 `CLIP_XY`로 중심 NDC를 컬링하는데, 1px 절두체 안에 중심이 들어온 splat만 남기 때문이다.

알아챈 방법은 실행 결과다. “가우시안 점의 센터만 잡힌다”가 그대로 재현됐다. `setViewOffset`을 빼고 **투영은 풀해상도, 커서는 1px scissor**로 바꿨다. 스플랫 크기는 `cameraViewport`(RT 크기)에 묶여 있어서 RT는 캔버스 크기를 유지해야 한다. 깊이는 알파 가중 누적(`splatPickMRT`)이다.

## 4. 핵심 프롬프트

**잘 된 것 — 병조와 목표를 프롬프트에 먼저 적음**

> 현재 three.js 가우시안스플랫 기본 raycast는 O(n)이라 몇백만 개 PLY에서 CPU 100%다. r186 Splat API를 먼저 보고, md 요구에 맞게 hover 시 겹친 가우시안의 기여도로 표면 점을 잡는 최선의 방식을 제안해.

코드를 시키기 전에 진단과 “기여도 표면”을 고정했다. 기본 raycast로 호버를 짜라는 요청이 원천적으로 막혔다.

**실패한 것 — 탐색 구조를 물었지, 맞출 표면을 못 적음**

> hover를 raycast로 하면 O(n)이라 CPU가 100%가 된다. 해시그리드 복셀로 가우시안을 찾으려 하는데, 그보다 나은 알고리즘이 있으면 비교해서 최선안을 제시해.

복잡도 진단은 맞았다. 다만 “무엇을 히트로 볼지”가 빠졌다. LLM은 3D 타원체–광선 교차 + 그리드를 짰고, 화면에 넓게 보이는 2D splat 원반은 가장자리에서 miss했다. 렌더는 투영된 가우시안이고 픽은 부피 교차라서다. 보이는 픽셀의 누적 깊이를 읽도록 바꿨다. 처음부터 “히트 = 화면에 그린 표면. 중심/타원체 교차는 제외”를 같이 적었어야 했다.
